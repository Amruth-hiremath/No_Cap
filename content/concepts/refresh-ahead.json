{
  "slug": "refresh-ahead",
  "version": 4,
  "title": "Refresh Ahead",
  "phase": "cache-aside",
  "area": "Caching",
  "estimated_minutes": 7,
  "difficulty": "advanced",
  "summary": "Refresh-ahead proactively refreshes popular cache entries before they expire, so reads of hot keys always hit a fresh value and never trigger a thundering herd. The cache itself (or a background worker) tracks TTL countdowns and re-fetches from the database a configurable margin before expiry. The result: hot keys never miss, latency is uniformly low, and stampede risk collapses — at the cost of background DB load and wasted refreshes for keys that are no longer hot.",
  "why_it_matters": "Cache stampedes on hot keys are the most common cause of production incidents in cached systems. When a viral post's TTL expires, thousands of concurrent readers all miss at once and stampede the database. Refresh-ahead is the canonical preventive pattern: instead of waiting for expiry, the cache refreshes hot entries in the background just before they would expire, so the foreground read path always sees a fresh hit. It is the strategy that powers news homepages, trending feeds, leaderboards, and any system where a single key can suddenly attract enormous read traffic. Understanding refresh-ahead means understanding how to make a cached system resilient to viral load.",
  "prerequisites": [
    "cache-aside"
  ],
  "related": [
    "write-through",
    "cache-aside",
    "cache-sizing"
  ],
  "used_in": [
    "News homepages and trending feeds.",
    "Leaderboards and ranking systems.",
    "Hot configuration values (feature flags, pricing tables).",
    "Viral content caches (social posts, video metadata).",
    "Recommendation pre-warming."
  ],
  "blocks": [
    {
      "type": "prose",
      "id": "intro",
      "payload": {
        "text": "In **refresh-ahead**, the cache proactively refreshes entries before their TTL expires. The classic trigger is a TTL threshold: if a key has a 60-second TTL, the system refreshes it when only 5 seconds of life remain. The refresh happens in the background — a worker thread or scheduler re-fetches the value from the DB and overwrites the cache entry, resetting the TTL. Foreground reads continue to hit the (slightly older but still valid) cache entry; they never see a miss and they never wait for a DB fetch.\n\nThis is the strategy that solves the cache stampede for read-only hot keys. Recall that write-through eliminates the stampede for *write-heavy* hot keys — the writes keep the cache warm. But for *read-only* hot keys (a viral post nobody is editing, a leaderboard, a homepage), there are no writes to keep the cache warm, and the TTL will eventually fire. Refresh-ahead is the answer: refresh before expiry, never let the cache miss for hot keys.\n\nThe trick is identifying which keys are hot. Refreshing every key before expiry defeats the purpose — most keys are never read again, and refreshing them is pure waste. The two common approaches are: (a) refresh only keys that have been read at least N times in the last interval (popularity tracking), and (b) refresh only keys whose TTL is about to expire *and* that were read recently (lazy refresh). Both aim to refresh only the keys that, if expired, would cause a stampede."
      }
    },
    {
      "type": "mermaid",
      "id": "flow",
      "payload": {
        "code": "flowchart TD\n    N0[\"single hot key, TTL=60s, refresh at 5s remaining\"]\n    N1[\"REFRESH\"]\n    N2[\"AHEAD\"]\n    N3[\"TIMELINE\"]\n    N4[\"(single\"]\n    N5[\"hot\"]\n    N6[\"key,\"]\n    N7[\"TTL\"]\n    N8[\"60s,\"]\n    N9[\"refresh\"]\n    N10[\"remaining)\"]\n    N11[\"cache: fresh\"]\n    N12[\"SET\"]\n    N13[\"60s\"]\n    N14[\"[cache:\"]\n    N15[\"fresh]\"]\n    N16[\"10s\"]\n    N17[\"READ\"]\n    N18[\"HIT\"]\n    N19[\"30s\"]\n    N20[\"cache: refreshed, TTL reset to 60s\"]\n    N21[\"55s\"]\n    N22[\"background\"]\n    N23[\"fires\"]\n    N24[\"refreshed,\"]\n    N25[\"reset\"]\n    N26[\"60s]\"]\n    N27[\"no miss!\"]\n    N28[\"(no\"]\n    N29[\"miss!)\"]\n    N30[\"70s\"]\n    N31[\"...\"]\n    N32[\"COMPARE\"]\n    N33[\"CACHE\"]\n    N34[\"ASIDE\"]\n    N35[\"WITHOUT\"]\n    N36[\"expires\"]\n    N37[\"1000\"]\n    N38[\"concurrent\"]\n    N39[\"READs\"]\n    N40[\"all\"]\n    N41[\"MISS\"]\n    N42[\"stampede!\"]\n    N43[\"queries\"]\n    N44[\"(stampede!)\"]\n    N45[\"cache\"]\n    N46[\"repopulated,\"]\n    N47[\"then\"]\n    N48[\"HITs\"]\n    N49[\"resume\"]\n    N50[\"KEY\"]\n    N51[\"PROPERTY:\"]\n    N52[\"ahead\"]\n    N53[\"trades\"]\n    N54[\"small\"]\n    N55[\"load\"]\n    N56[\"for\"]\n    N57[\"the\"]\n    N58[\"absence\"]\n    N59[\"stampede\"]\n    N60[\"misses\"]\n    N61[\"keys.\"]\n    N0 --> N1\n    N1 --> N2\n    N2 --> N3\n    N3 --> N4\n    N4 --> N5\n    N5 --> N6\n    N6 --> N7\n    N7 --> N8\n    N8 --> N9\n    N9 --> N10\n    N10 --> N11\n    N11 --> N12\n    N12 --> N13\n    N13 --> N14\n    N14 --> N15\n    N15 --> N16\n    N16 --> N17\n    N17 --> N18\n    N18 --> N19\n    N19 --> N20\n    N20 --> N21\n    N21 --> N22\n    N22 --> N23\n    N23 --> N24\n    N24 --> N25\n    N25 --> N26\n    N26 --> N27\n    N27 --> N28\n    N28 --> N29\n    N29 --> N30\n    N30 --> N31\n    N31 --> N32\n    N32 --> N33\n    N33 --> N34\n    N34 --> N35\n    N35 --> N36\n    N36 --> N37\n    N37 --> N38\n    N38 --> N39\n    N39 --> N40\n    N40 --> N41\n    N41 --> N42\n    N42 --> N43\n    N43 --> N44\n    N44 --> N45\n    N45 --> N46\n    N46 --> N47\n    N47 --> N48\n    N48 --> N49\n    N49 --> N50\n    N50 --> N51\n    N51 --> N52\n    N52 --> N53\n    N53 --> N54\n    N54 --> N55\n    N55 --> N56\n    N56 --> N57\n    N57 --> N58\n    N58 --> N59\n    N59 --> N60\n    N60 --> N61",
        "caption": "Refresh-ahead refreshes a hot key at t=55s (TTL=60s) so the t=60s reads never miss.",
        "alt_text": "Refresh-ahead timeline diagram. A hot key is set at time zero with a 60-second TTL. Reads at time 10 and time 30 hit the cache. At time 55, five seconds before expiry, a background refresh fires: the cache re-fetches the value from the database and resets the TTL to 60 seconds. Reads at time 60 and 70 still hit the cache — no miss. Compare to cache-aside without refresh-ahead: the same key expires at time 60, and 1000 concurrent reads at time 60 all miss and stampede the database with 1000 simultaneous queries. The key property is that refresh-ahead trades a small background database load for the absence of stampede misses on hot keys."
      }
    },
    {
      "type": "prose",
      "id": "trigger",
      "payload": {
        "text": "**Three ways to trigger the refresh.**\n\nThe refresh-ahead scheduler can be triggered in three ways, with different trade-offs:\n\n1. **Time-based (TTL margin).** Each key has a TTL; a background scheduler periodically scans for keys whose TTL is below the refresh threshold (e.g., 5 seconds remaining) and refreshes them. Simple, but scans every key — wasteful if most keys are cold.\n\n2. **Popularity-based.** Track per-key read counts in a sliding window. Refresh only keys above a popularity threshold. This focuses refresh work on keys that actually cause stampedes, but the popularity tracking itself is a data structure you have to maintain (often a sorted set or a count-min sketch).\n\n3. **Read-triggered (lazy refresh).** On a read, if the TTL is below the refresh threshold, return the cached value *and* trigger an async refresh. The current read still hits; the next read sees a fresh value. This is the cheapest to implement because it requires no background scheduler — refresh is piggybacked on real reads. The downside is that the *first* read after the threshold sees the slightly stale value (though not as stale as cache-aside).\n\nMost production systems use (2) or (3). Variant (3) — sometimes called *stale-while-revalidate* at the cache level — is built into HTTP's `Cache-Control: stale-while-revalidate` and into CDN behaviors, and is the easiest to add to an existing cache-aside system."
      }
    },
    {
      "type": "callout",
      "id": "swr",
      "payload": {
        "title": "stale-while-revalidate is refresh-ahead for HTTP",
        "body": "The HTTP Cache-Control directive `stale-while-revalidate=30` says: \"after this response becomes stale, you may serve it for up to 30 more seconds while you revalidate in the background.\" That is exactly refresh-ahead with a 30-second refresh window. CDNs (Cloudflare, CloudFront, Fastly) implement this natively. It is the cheapest possible refresh-ahead: no popularity tracking, no background scheduler, just a per-response directive. If your cache layer is a CDN or HTTP cache, this is the first thing to reach for.",
        "kind": "info"
      }
    },
    {
      "type": "prose",
      "id": "waste",
      "payload": {
        "text": "**The waste problem and the cold-key problem.**\n\nRefresh-ahead has two failure modes that pull in opposite directions:\n\n- **Over-refreshing:** if you refresh every key before expiry, you spend DB load on keys that are never read again. A key that was hot an hour ago but is now cold still gets refreshed — pure waste. The cost scales with the size of the cache, not with the read traffic.\n- **Under-refreshing:** if you only refresh popular keys, a key that suddenly becomes viral in the gap between refresh and expiry still stampedes. Refresh-ahead protects against steady-state hot keys, not against sudden spikes.\n\nThe standard mitigation for over-refreshing is to combine refresh-ahead with a popularity signal: only refresh keys that have been read at least N times in the last interval. This bounds the refresh work to the actual hot set. The standard mitigation for under-refreshing (sudden spikes) is to also have stampede protection on the read path — cache locking (single-flight) or probabilistic early expiration — so that even if a key slips through, the stampede is bounded to one DB fetch.\n\nThe deeper insight is that **refresh-ahead optimizes for steady-state hot keys, not for spikes.** If your access pattern is bursty — a key goes from 0 reads to 10,000 reads in a second — refresh-ahead will not help, because the key was not popular before the burst. For bursty traffic, you need stampede protection (single-flight) plus a short TTL, or pre-warming based on predictive signals."
      }
    },
    {
      "type": "prose",
      "id": "when-to-use",
      "payload": {
        "text": "**When to use refresh-ahead:**\n- **Hot, read-only keys with steady-state traffic.** Leaderboards, trending lists, homepage feeds.\n- **Keys whose expiry causes stampedes you have observed in production.** Don't add it preemptively — add it when you see the stampede.\n- **HTTP/CDN-cached responses.** `stale-while-revalidate` is free; use it.\n- **When you can identify the hot set in advance.** Pre-warming at scheduled times (e.g., refresh homepage every minute during peak hours).\n\n**When NOT to use refresh-ahead:**\n- **When keys are rarely read twice.** Refreshing them is pure waste — let them expire and let cache-aside re-populate on the rare read.\n- **When access patterns are bursty.** Refresh-ahead protects steady-state, not spikes. Use single-flight instead.\n- **When the DB cannot handle the refresh load.** Refresh-ahead adds background DB load proportional to the hot set size; if the DB is already at capacity, this hurts rather than helps.\n- **When freshness does not matter.** If a 5-minute stale window is fine, cache-aside with TTL is simpler and equally effective."
      }
    },
    {
      "type": "quiz",
      "id": "q1",
      "payload": {
        "question": "A leaderboard key has a 60-second TTL and is being read 5,000 times/sec. Without refresh-ahead, what happens at t=60s?",
        "shape": "mcq",
        "options": [
          "The cache silently extends the TTL.",
          "All 5,000 concurrent reads at t=60s miss and stampede the database, briefly producing 5,000 simultaneous DB queries.",
          "The cache automatically refreshes from the DB.",
          "Reads return an error until the cache is repopulated."
        ],
        "answer_index": 1,
        "rationale": "Without refresh-ahead or stampede protection, the cache key expires at t=60s. The next reads all see a miss simultaneously — all 5,000 readers in that second issue a DB query. This is the cache stampede, and it is exactly what refresh-ahead prevents: by refreshing at, say, t=55s in the background, the foreground reads at t=60s still hit a fresh cached value. The alternative mitigations are single-flight (only one reader fetches, others wait) or probabilistic early expiration (jitter TTLs so they don't all expire at once). For a known-hot key like a leaderboard, refresh-ahead is the cleanest solution.",
        "difficulty": "core"
      }
    },
    {
      "type": "quiz",
      "id": "q2",
      "payload": {
        "question": "You enable refresh-ahead on every key in the cache. What problem will you observe, and how do you fix it?",
        "shape": "mcq",
        "options": [
          "No problem — refresh-ahead is always beneficial.",
          "Most of the refresh work is wasted on cold keys that are never read again, adding unnecessary DB load. Fix by refreshing only keys above a popularity threshold.",
          "The cache will run out of memory because refresh-ahead doubles the size of every entry.",
          "The DB will run out of connections because refresh-ahead opens one per key."
        ],
        "answer_index": 1,
        "rationale": "Refresh-ahead's defining waste problem is over-refreshing. Most keys in a typical cache are cold — they were written once and read once, and will never be read again. Refreshing them before expiry is pure DB load for zero benefit. The fix is to gate refresh on a popularity signal: only refresh keys with at least N reads in the last interval, or only refresh keys that were read in the last few seconds. This focuses refresh work on the keys that, if expired, would actually cause a stampede. Over-refreshing is the most common operational mistake when first enabling refresh-ahead, and the most common reason teams abandon it as \"too expensive.\"",
        "difficulty": "solid"
      }
    },
    {
      "type": "quiz",
      "id": "q3",
      "payload": {
        "question": "A previously-cold key suddenly becomes viral and gets 100,000 reads/sec. You have refresh-ahead enabled with popularity tracking. Will refresh-ahead save you from the stampede when the TTL expires?",
        "shape": "mcq",
        "options": [
          "Yes — refresh-ahead protects against all stampedes.",
          "No — the key was cold before the burst, so refresh-ahead was not refreshing it; the TTL will expire and the burst of reads will stampede the DB. You need single-flight or pre-warming.",
          "Only if the popularity threshold is set very low (e.g., 1 read).",
          "Yes, but only if the refresh interval is below 1 second."
        ],
        "answer_index": 1,
        "rationale": "Refresh-ahead optimizes for steady-state hot keys, not for spikes. A key that was cold before the burst had no popularity signal, so refresh-ahead was not refreshing it. When the TTL expires during the burst, all 100,000 concurrent readers miss at once — exactly the stampede refresh-ahead is supposed to prevent. The right mitigations for bursty traffic are (a) single-flight / request coalescing on the read path, so only one reader fetches from the DB and others wait, and (b) predictive pre-warming based on external signals (e.g., when a post starts trending on another platform). Refresh-ahead is a steady-state optimization; it cannot retroactively protect a key that became hot in the last second.",
        "difficulty": "interview"
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-refresh-ahead-mental",
      "payload": {
        "text": "Engineering mental model",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-refresh-ahead-mental",
      "payload": {
        "text": "**Mental model.** Think of Refresh Ahead as a deliberate boundary in a system. The boundary exists because something becomes harder to manage when everything is done in one place: latency, scale, failure isolation, consistency, cost, or team ownership. The useful question is not “what does Refresh Ahead mean?” but “what pressure makes this boundary worth introducing, and what new failure mode does it create?”"
      }
    },
    {
      "type": "callout",
      "id": "upgrade-callout-refresh-ahead-decision",
      "payload": {
        "title": "Design lens",
        "body": "Before choosing Refresh Ahead, name the workload, the critical user path, the dominant bottleneck, the failure you are trying to absorb, and the trade-off you are willing to accept. If you cannot name those five things, the design is probably premature.",
        "kind": "info"
      }
    },
    {
      "type": "image",
      "id": "upgrade-image-refresh-ahead",
      "payload": {
        "src": "/learning-visuals/refresh-ahead.svg",
        "alt": "Original NO CAP systems visual for Refresh Ahead.",
        "caption": "Refresh Ahead: a compact system-thinking visual.",
        "credit": "Original NO CAP visual.",
        "source_url": "https://github.com/donnemartin/system-design-primer"
      }
    },
    {
      "type": "mermaid",
      "id": "upgrade-mermaid-refresh-ahead",
      "payload": {
        "code": "flowchart LR\n  A[Client] --> B[Gateway] --> C[Service] --> D[State] --> E[Outcome]",
        "caption": "Refresh Ahead: a simplified system view showing the primary flow and the main state boundaries.",
        "alt_text": "Refresh Ahead: a simplified system view showing the primary flow and the main state boundaries."
      }
    },
    {
      "type": "code",
      "id": "upgrade-code-refresh-ahead",
      "payload": {
        "language": "text",
        "code": "// Pseudocode\nrequest = receive()\nresult = refresh_ahead(request)\nreturn result\n\n// Production questions:\n// 1. What happens on timeout?\n// 2. Can this operation be retried safely?\n// 3. What is the bottleneck?",
        "caption": "A minimal engineering sketch for reasoning about Refresh Ahead."
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-refresh-ahead-numbers",
      "payload": {
        "text": "Back-of-the-envelope reasoning",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-refresh-ahead-numbers",
      "payload": {
        "text": "Numerical lens: write down traffic, payload size, read/write ratio, peak multiplier and durability target before choosing a component. The numbers should justify the architecture."
      }
    },
    {
      "type": "simulation",
      "id": "upgrade-sim-refresh-ahead",
      "payload": {
        "title": "Interactive thought experiment: Refresh Ahead",
        "description": "Change the variables below and predict what breaks first in Refresh Ahead. The production lab can later reuse these same inputs.",
        "variables": [
          {
            "name": "traffic",
            "description": "Requests/events per second",
            "default": "1000"
          },
          {
            "name": "latency",
            "description": "Dependency latency",
            "default": "50ms"
          },
          {
            "name": "failure_rate",
            "description": "Injected failure rate",
            "default": "0%"
          }
        ],
        "placeholder_note": "Interactive controls are deterministic; use the linked Labs section when a dedicated lab exists."
      }
    },
    {
      "type": "callout",
      "id": "upgrade-callout-refresh-ahead-hint",
      "payload": {
        "title": "Hint",
        "body": "If you are stuck on Refresh Ahead, start by drawing the request path and marking every network hop, stateful component, queue, cache and failure boundary. Then estimate where the system will saturate.",
        "kind": "note"
      }
    },
    {
      "type": "quiz",
      "id": "upgrade-quiz-refresh-ahead-1",
      "payload": {
        "shape": "prediction",
        "question": "You increase traffic by 10× in a system using Refresh Ahead. What should you inspect first?",
        "options": [
          "The newest framework version",
          "The dominant bottleneck and its saturation signal",
          "Only the UI",
          "Nothing; the architecture should absorb any traffic automatically"
        ],
        "answer_index": 1,
        "rationale": "System design is a constraint-and-bottleneck exercise. Start with the resource or dependency that will saturate first, then choose the smallest change that removes that bottleneck.",
        "difficulty": "solid"
      }
    },
    {
      "type": "quiz",
      "id": "upgrade-quiz-refresh-ahead-2",
      "payload": {
        "shape": "mcq",
        "question": "Which statement is the safest engineering habit when using Refresh Ahead?",
        "options": [
          "Choose it because it is popular",
          "Add it before measuring the problem",
          "State the workload, failure mode and trade-off it is solving",
          "Use the most distributed design possible"
        ],
        "answer_index": 2,
        "rationale": "Good architecture starts from requirements, workload and failure assumptions. The component is the consequence of that reasoning, not the starting point.",
        "difficulty": "interview"
      }
    },
    {
      "type": "scenario",
      "id": "upgrade-scenario-refresh-ahead",
      "payload": {
        "prompt": "Production scenario: your system uses Refresh Ahead, traffic suddenly spikes, and p99 latency doubles. What is your first move?",
        "context": "You have dashboards for traffic, latency, errors and saturation. You can change the architecture, but every change has operational cost.",
        "options": [
          {
            "id": "a",
            "text": "Add more instances immediately",
            "outcome": "This may help, but only if compute saturation is the bottleneck.",
            "correct": false
          },
          {
            "id": "b",
            "text": "Inspect the bottleneck and its saturation signal first",
            "outcome": "Correct: diagnose before scaling or introducing a new pattern.",
            "correct": true
          },
          {
            "id": "c",
            "text": "Disable monitoring to reduce overhead",
            "outcome": "This removes the evidence you need to diagnose the incident.",
            "correct": false
          }
        ],
        "rationale": "Measure the limiting resource or dependency first. A bottleneck can be compute, database I/O, network, cache misses, lock contention, downstream latency, or queue backlog.",
        "difficulty": "interview"
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-refresh-ahead-interview",
      "payload": {
        "text": "Interview drill",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-refresh-ahead-interview",
      "payload": {
        "text": "**Answer this without notes:** When would you choose Refresh Ahead, and when would you intentionally avoid it? Mention at least one bottleneck it addresses, one failure mode it introduces, and one alternative. Then quantify the workload you are designing for."
      }
    },
    {
      "type": "callout",
      "id": "field-guide-refresh-ahead",
      "payload": {
        "title": "Engineering lens",
        "body": "For Refresh Ahead, reason in this order: what data is hottest, where can it live closer to the caller, what makes it stale, and what happens on a miss or cache failure. A cache is an optimization boundary, not the source of truth.",
        "kind": "tip"
      }
    },
    {
      "type": "prose",
      "id": "numerical-lens-refresh-ahead",
      "payload": {
        "text": "### Numerical sanity check\n\nA useful first-order model is cache_load = request_rate × (1 - hit_rate). If traffic is 20,000 req/s and the hit rate is 90%, roughly 2,000 req/s still reaches the origin before considering misses caused by expiration or eviction."
      }
    },
    {
      "type": "quiz",
      "id": "field-question-refresh-ahead",
      "payload": {
        "shape": "prediction",
        "question": "You have a read-heavy Refresh Ahead path. Traffic doubles overnight. What metric would you inspect first, and what would convince you to add another cache layer?",
        "context": "Do not optimize for a memorized definition. Reason from the workload and failure mode.",
        "options": [
          "Scale the same component immediately.",
          "Measure the current bottleneck and state the assumption that changed.",
          "Add more technologies until throughput improves.",
          "Choose the most popular architecture."
        ],
        "answer_index": 1,
        "rationale": "Good system design starts with the bottleneck and the changed constraint. The most popular or complex choice is not automatically correct.",
        "difficulty": "interview"
      }
    }
  ],
  "trade_offs": {
    "pros": [
      "Eliminates the cache stampede for steady-state hot keys.",
      "Reads of hot keys are uniformly fast (never miss, never wait for DB).",
      "Pairs naturally with `stale-while-revalidate` for HTTP/CDN caches — free at the protocol level.",
      "Refreshes can be scheduled off-peak to smooth DB load.",
      "Combined with popularity tracking, refresh work is bounded to the actual hot set."
    ],
    "cons": [
      "Wastes DB load refreshing cold keys unless popularity tracking is added.",
      "Does not protect against bursty spikes (a key that was cold and suddenly becomes viral).",
      "Adds operational complexity — a background scheduler, popularity tracking, monitoring of refresh lag.",
      "Refresh work scales with the hot set size, not with read traffic — a large hot set adds steady DB load.",
      "The hot set must be reasonably stable; rapidly shifting hot sets defeat the popularity signal."
    ]
  },
  "failure_modes": [
    "Over-refreshing cold keys — pure DB load waste, often the first thing teams hit.",
    "Sudden viral spike on a previously-cold key — refresh-ahead was not protecting it, stampede occurs.",
    "Refresh scheduler falling behind — keys expire before the scheduler reaches them, defeating the purpose.",
    "Popularity tracking itself becoming a bottleneck — sorted set or count-min sketch memory growth.",
    "Refresh failure not retried — key expires silently and stampedes on the next read.",
    "Clock skew between cache and scheduler — refresh fires late, key expires first."
  ],
  "common_mistakes": [
    "Enabling refresh-ahead on every key without popularity gating — pure waste.",
    "Assuming refresh-ahead protects against spikes — it protects against steady-state expiry, not bursts.",
    "Not having a fallback (single-flight) when refresh fails — the stampede returns.",
    "Setting the refresh threshold too close to TTL expiry (e.g., 1 second before) — a slow refresh causes a miss.",
    "Setting the refresh threshold too far from TTL expiry (e.g., 30 seconds before a 60s TTL) — wastes half the cached value's lifetime.",
    "Forgetting that `stale-while-revalidate` already implements refresh-ahead at the HTTP layer — duplicate work."
  ],
  "where_you_see_it": [
    "News homepages (BBC, NYT, CNN) — refresh headline caches every minute during peak hours.",
    "Leaderboards (gaming, fitness apps) — refresh-ahead on the top-N entries.",
    "Trending feeds (Twitter, Reddit) — refresh-ahead on the trending list, with single-flight fallback.",
    "CDN edge caches — `stale-while-revalidate` is refresh-ahead at the HTTP layer.",
    "Configuration and feature-flag services — refresh-ahead to keep hot config values fresh."
  ],
  "interview_prompts": [
    "Your news homepage gets 50,000 reads/sec and the cache TTL is 60s. At t=60s, the cache expires and the DB is overwhelmed. How do you fix this?",
    "Compare refresh-ahead, single-flight, and probabilistic early expiration. When would you choose each?",
    "You enabled refresh-ahead on every cache key and your DB load went up 30%. Why? How do you fix it?",
    "A viral post appears suddenly and your cache misses for the first 10 seconds. Refresh-ahead was enabled. Why didn't it help?",
    "How does HTTP `stale-while-revalidate` relate to refresh-ahead? Where does it not apply?"
  ],
  "real_system_mappings": [
    {
      "system": "Cloudflare / Fastly CDN edge caches",
      "how": "Both CDNs implement `stale-while-revalidate` natively, which is refresh-ahead at the HTTP layer: a stale response is served immediately while the edge revalidates against the origin in the background. The next request sees a fresh response with no origin round-trip."
    },
    {
      "system": "Twitter / X timeline caches",
      "how": "Hot timeline caches (the homepage for high-traffic accounts) use refresh-ahead with popularity-based triggering — only the top N most-read timelines are refreshed before expiry, bounding refresh work to the actual hot set."
    },
    {
      "system": "Reddit ranking / leaderboard caches",
      "how": "Front-page ranking caches are refreshed ahead of TTL during peak hours to ensure reads always hit, with single-flight fallback for keys whose refresh fails or is delayed."
    },
    {
      "system": "Discord presence / channel caches",
      "how": "Hot channel state (recent activity, online member counts) is refreshed ahead of TTL during high-traffic events (large streams, server raids) to keep read latency uniform."
    }
  ],
  "status": "published",
  "sources": [
    {
      "title": "System Design Primer",
      "url": "https://github.com/donnemartin/system-design-primer",
      "publisher": "Open source"
    },
    {
      "title": "ByteByteGo — Scale from zero to millions",
      "url": "https://bytebytego.com/courses/system-design-interview/scale-from-zero-to-millions-of-users",
      "publisher": "ByteByteGo"
    },
    {
      "title": "System Design Tutorial",
      "url": "https://www.geeksforgeeks.org/system-design/system-design-tutorial/",
      "publisher": "GeeksforGeeks"
    },
    {
      "title": "System Design Roadmap",
      "url": "https://roadmap.sh/system-design",
      "publisher": "roadmap.sh"
    },
    {
      "title": "Caching reference",
      "url": "https://bytebytego.com/courses/system-design-interview/scale-from-zero-to-millions-of-users",
      "publisher": "Reference"
    },
    {
      "title": "Caching reference",
      "url": "https://aws.amazon.com/caching/",
      "publisher": "Reference"
    },
    {
      "title": "Caching reference",
      "url": "https://redis.io/docs/latest/develop/get-started/",
      "publisher": "Reference"
    }
  ]
}
