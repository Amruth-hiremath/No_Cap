{
  "slug": "design-notification-system",
  "version": 3,
  "title": "Design Notification System",
  "phase": "case-studies",
  "area": "Case Studies",
  "estimated_minutes": 12,
  "difficulty": "interview",
  "summary": "Design a multi-channel notification system (iOS/Android push, SMS, email, in-app) that fans a single event out to many channels and many recipients. Covers the event bus -> fan-out worker pattern, per-provider rate limiting (APNS/FCM, Twilio, SES), template rendering with localization, idempotency keys to defeat retries, preference & quiet-hours enforcement, and the priority queue that keeps 'your ride is here' ahead of '20% off shoes'.",
  "why_it_matters": "Every modern product sends notifications — OTPs, transactional alerts, marketing pushes, social mentions. A naive system either (a) sends through one channel and ignores users who only open email, or (b) blasts every channel and burns sender reputation plus user goodwill. The hard parts are not the HTTP call to APNS — they are fan-out across heterogeneous providers with different rate limits, deduplication under retries, and respect for user preferences. This design is the spine of every notification platform at Uber, Slack, Airbnb, and Stripe, and the channel-fan-out + per-provider rate-limit pattern shows up in dozens of system-design interviews.",
  "prerequisites": [
    "message-queues",
    "pub-sub",
    "rate-limiting",
    "back-pressure"
  ],
  "related": [
    "design-rate-limiter",
    "pub-sub",
    "message-queues"
  ],
  "used_in": [
    "Uber (push notifications for trip updates)",
    "Slack (notifications across desktop/mobile/email)",
    "Airbnb (booking confirmations via SMS+email+push)",
    "Stripe (transactional receipts)"
  ],
  "blocks": [
    {
      "type": "prose",
      "id": "problem",
      "payload": {
        "text": "**What are we designing?** A platform that takes a single semantic event from any product service (\"order #1234 shipped\", \"@alice mentioned you\", \"your OTP is 4471\") and routes it to one or more delivery channels per the recipient's preferences. The same event may become an iOS push, an Android push, an SMS, an email, and a banner in the web app — possibly all five, possibly none if the user is in quiet hours.\n\nThe defining challenge is **heterogeneous fan-out under constraint**. Every channel has different throughput limits, latency, cost, and failure semantics. APNS will throttle you, Twilio charges per message, SES will suspend your account if bounce rate exceeds 5%. The system must orchestrate all of this while never sending a duplicate notification and always honoring the user's preferences."
      }
    },
    {
      "type": "prose",
      "id": "requirements",
      "payload": {
        "text": "**Functional requirements.**\n- A product service publishes a notification event (recipient, type, payload, channels).\n- The system delivers the event through the requested channels (or the user's configured defaults).\n- Users can set per-channel preferences (e.g. \"SMS only for security alerts, push for everything else, no marketing email ever\").\n- Users can set quiet hours (no push 22:00-07:00 local).\n- The system tracks delivery status (sent, delivered, failed, opened).\n- Notifications are deduplicated: a retried producer must not cause two SMSes.\n\n**Non-functional requirements.**\n- **End-to-end latency**: p99 < 30 s for transactional, < 5 min for marketing.\n- **Throughput**: 10K notifications/sec average, 100K/sec peak (Black Friday, OTP storms).\n- **At-least-once delivery** with idempotent consumers (duplicates are tolerable in rare retry windows but must be the exception).\n- **No notification storms**: a buggy producer must not blast 1M users.\n- **Provider reputation protection**: bounce/complaint rate < 0.5%.\n\n**Non-goals.** No real-time chat (that's design-chat-system), no in-product marketing campaign orchestration (separate CRM tool), no rich-media push with images (v1)."
      }
    },
    {
      "type": "prose",
      "id": "capacity",
      "payload": {
        "text": "**Capacity estimation.**\n\n*Volume.* 10K notifications/sec avg = ~860M/day. Peak 100K/sec during OTP storms or marketing blasts. ~80% push, 15% email, 5% SMS.\n\n*Storage.* Each notification record ~1KB (recipient, channel, status, timestamps, payload_hash). 860M/day x 1KB = ~860 GB/day = ~315 TB/year. Hot retention 30 days (~26 TB) in a searchable store; cold archive to S3 after.\n\n*Provider rate limits (rough).*\n- APNS: ~9,000 notifications/sec per cert; we shard across multiple certs/apps.\n- FCM: similar, ~10K/sec per project.\n- SES: 14 req/sec per region out of sandbox; production limits scale by request.\n- Twilio SMS: 10-100 messages/sec per long code, 250/sec per shortcode; throughput varies by country.\n\n*Cost (rough order of magnitude).* Push ~free (APNS/FCM don't charge). Email ~$0.10 per 1000 messages via SES. SMS varies wildly by country: $0.005-$0.05 per message. A 100M-user marketing SMS campaign is $500K-$5M. This is why SMS is reserved for high-value transactional events."
      }
    },
    {
      "type": "prose",
      "id": "apis",
      "payload": {
        "text": "**APIs.**\n\n```\nPOST /v1/notifications                  -- send a notification\n  { recipient_id, event_type, channels: [...],\n    payload: { template_id, vars: {...} },\n    idempotency_key, priority }\n\nGET  /v1/notifications/:id/status        -- delivery status across channels\nPOST /v1/preferences/:user_id            -- set channel preferences + quiet hours\nGET  /v1/preferences/:user_id\n```\n\nThe producer-supplied **idempotency_key** is critical. Producers are usually pub/sub consumers that redeliver on failure. Without dedup, a single retry could double every SMS. The notification service hashes (idempotency_key, channel) and rejects duplicates within a 24-hour window using a Redis SET with TTL."
      }
    },
    {
      "type": "prose",
      "id": "data-model",
      "payload": {
        "text": "**Data model.**\n\n*Events* (Kafka topic, partitioned by recipient_id):\n```\ntopic: notifications\nevent: { id, recipient_id, event_type, payload, channels,\n         priority, idempotency_key, created_at }\nnpartitions = 64    -- consumer parallelism\n```\n\n*Notifications* (DynamoDB / sharded SQL, key by recipient_id+created_at for fast per-user history):\n```\nnotifications (\n  id PK, recipient_id, event_type, channel, status,\n  payload_hash, idempotency_key, created_at, sent_at, delivered_at\n  INDEX (recipient_id, created_at)\n  INDEX (idempotency_key, channel)            -- dedup lookup\n)\n```\n\n*Preferences* (per-user):\n```\npreferences (\n  user_id PK, channel_prefs JSON,\n  quiet_hours JSON, locale, timezone\n)\n```\n\n*Per-channel rate-limit state* (Redis, per provider):\n```\nkey:   ratelimit:apns:cert_1\ntype:  token bucket (Lua script) -- 9000 tokens/sec, burst 18000\n```\n\n*Templates* (versioned in Git / DB):\n```\ntemplates (id PK, version, channel, locale, body_jinja2, vars_schema)\n```\nTemplates are versioned so a render with v1 of a template is deterministic; rolling out v2 doesn't retroactively change history."
      }
    },
    {
      "type": "mermaid",
      "id": "arch",
      "payload": {
        "code": "flowchart TD\n    N0[\"Product Service\"]\n    N1[\"Auth Service\"]\n    N2[\"Marketing Service\"]\n    N3[\"[Product\"]\n    N4[\"Service]\"]\n    N5[\"[Auth\"]\n    N6[\"[Marketing\"]\n    N7[\"idempotency_key\"]\n    N8[\"POST\"]\n    N9[\"notifications\"]\n    N10[\"(idempotency_key)\"]\n    N11[\"Notification API\"]\n    N12[\"[Notification\"]\n    N13[\"API]\"]\n    N14[\"Redis SET, 24h TTL\"]\n    N15[\"dedup\"]\n    N16[\"check\"]\n    N17[\"(Redis\"]\n    N18[\"SET,\"]\n    N19[\"24h\"]\n    N20[\"TTL)\"]\n    N21[\"cache-aside\"]\n    N22[\"load\"]\n    N23[\"preferences\"]\n    N24[\"(cache\"]\n    N25[\"aside)\"]\n    N26[\"resolve\"]\n    N27[\"channels\"]\n    N28[\"per\"]\n    N29[\"prefs\"]\n    N30[\"quiet\"]\n    N31[\"hours\"]\n    N32[\"produce\"]\n    N33[\"event\"]\n    N34[\"Kafka\"]\n    N35[\"Kafka: notifications topic, 64 partitions\"]\n    N36[\"[Kafka:\"]\n    N37[\"topic,\"]\n    N38[\"partitions]\"]\n    N39[\"Push Fan-out\"]\n    N40[\"Email Fan-out\"]\n    N41[\"SMS Fan-out\"]\n    N42[\"[Push\"]\n    N43[\"Fan\"]\n    N44[\"out]\"]\n    N45[\"[Email\"]\n    N46[\"[SMS\"]\n    N47[\"priority queue\"]\n    N48[\"batched, SES\"]\n    N49[\"rate-limited\"]\n    N50[\"(priority\"]\n    N51[\"queue)\"]\n    N52[\"(batched,\"]\n    N53[\"SES)\"]\n    N54[\"(rate\"]\n    N55[\"limited)\"]\n    N56[\"Render Template\"]\n    N57[\"Render+Inline\"]\n    N58[\"Render+Localize\"]\n    N59[\"[Render\"]\n    N60[\"Template]\"]\n    N61[\"Inline]\"]\n    N62[\"Localize]\"]\n    N63[\"locale\"]\n    N64[\"aware\"]\n    N65[\"jinja2\"]\n    N66[\"country\"]\n    N67[\"deeplink\"]\n    N68[\"vars\"]\n    N69[\"tracking\"]\n    N70[\"pixel\"]\n    N71[\"sender\"]\n    N72[\"Per-channel Rate Limiter (Redis token bucket)\"]\n    N73[\"Redis token bucket\"]\n    N74[\"[Per\"]\n    N75[\"channel\"]\n    N76[\"Rate\"]\n    N77[\"Limiter\"]\n    N78[\"token\"]\n    N79[\"bucket)]\"]\n    N80[\"APNS / FCM\"]\n    N81[\"SES / SendGrid\"]\n    N82[\"Twilio\"]\n    N83[\"[APNS\"]\n    N84[\"FCM]\"]\n    N85[\"[SES\"]\n    N86[\"SendGrid]\"]\n    N87[\"[Twilio]\"]\n    N88[\"Callback Webhook\"]\n    N89[\"Status Updater\"]\n    N90[\"[Callback\"]\n    N91[\"Webhook]\"]\n    N92[\"[Status\"]\n    N93[\"Updater]\"]\n    N94[\"delivery\"]\n    N95[\"receipts\"]\n    N96[\"mark\"]\n    N97[\"sent\"]\n    N98[\"delivered\"]\n    N99[\"failed\"]\n    N100[\"bounce\"]\n    N101[\"complaint\"]\n    N102[\"bump\"]\n    N103[\"rate\"]\n    N104[\"metric\"]\n    N105[\"Dead-letter queue\"]\n    N106[\"[Dead\"]\n    N107[\"letter\"]\n    N108[\"queue]\"]\n    N109[\"exhausted\"]\n    N110[\"retries\"]\n    N111[\"Alert if bounce rate > 0.5%\"]\n    N112[\"[Alert\"]\n    N113[\"0.5%]\"]\n    N0 --> N1\n    N1 --> N2\n    N2 --> N3\n    N3 --> N4\n    N4 --> N5\n    N5 --> N6\n    N6 --> N7\n    N7 --> N8\n    N8 --> N9\n    N9 --> N10\n    N10 --> N11\n    N11 --> N12\n    N12 --> N13\n    N13 --> N14\n    N14 --> N15\n    N15 --> N16\n    N16 --> N17\n    N17 --> N18\n    N18 --> N19\n    N19 --> N20\n    N20 --> N21\n    N21 --> N22\n    N22 --> N23\n    N23 --> N24\n    N24 --> N25\n    N25 --> N26\n    N26 --> N27\n    N27 --> N28\n    N28 --> N29\n    N29 --> N30\n    N30 --> N31\n    N31 --> N32\n    N32 --> N33\n    N33 --> N34\n    N34 --> N35\n    N35 --> N36\n    N36 --> N37\n    N37 --> N38\n    N38 --> N39\n    N39 --> N40\n    N40 --> N41\n    N41 --> N42\n    N42 --> N43\n    N43 --> N44\n    N44 --> N45\n    N45 --> N46\n    N46 --> N47\n    N47 --> N48\n    N48 --> N49\n    N49 --> N50\n    N50 --> N51\n    N51 --> N52\n    N52 --> N53\n    N53 --> N54\n    N54 --> N55\n    N55 --> N56\n    N56 --> N57\n    N57 --> N58\n    N58 --> N59\n    N59 --> N60\n    N60 --> N61\n    N61 --> N62\n    N62 --> N63\n    N63 --> N64\n    N64 --> N65\n    N65 --> N66\n    N66 --> N67\n    N67 --> N68\n    N68 --> N69\n    N69 --> N70\n    N70 --> N71\n    N71 --> N72\n    N72 --> N73\n    N73 --> N74\n    N74 --> N75\n    N75 --> N76\n    N76 --> N77\n    N77 --> N78\n    N78 --> N79\n    N79 --> N80\n    N80 --> N81\n    N81 --> N82\n    N82 --> N83\n    N83 --> N84\n    N84 --> N85\n    N85 --> N86\n    N86 --> N87\n    N87 --> N88\n    N88 --> N89\n    N89 --> N90\n    N90 --> N91\n    N91 --> N92\n    N92 --> N93\n    N93 --> N94\n    N94 --> N95\n    N95 --> N96\n    N96 --> N97\n    N97 --> N98\n    N98 --> N99\n    N99 --> N100\n    N100 --> N101\n    N101 --> N102\n    N102 --> N103\n    N103 --> N104\n    N104 --> N105\n    N105 --> N106\n    N106 --> N107\n    N107 --> N108\n    N108 --> N109\n    N109 --> N110\n    N110 --> N111\n    N111 --> N112\n    N112 --> N113",
        "caption": "Notification system: producers POST events, API dedupes + resolves preferences + produces to Kafka, per-channel fan-out workers render templates, rate-limit against the provider, and send; callbacks update status.",
        "alt_text": "Diagram of a multi-channel notification system. Product, Auth, and Marketing services POST events to the Notification API. The API deduplicates using a Redis SET with 24-hour TTL keyed by idempotency_key plus channel, loads the recipient's preferences from a cache, resolves which channels to actually use based on preferences and quiet hours, and produces an event onto a Kafka topic with 64 partitions. Three independent fan-out worker pools consume from Kafka: one for push notifications using a priority queue, one for email batched through SES, one for SMS through Twilio with strict rate limiting. Each worker renders the appropriate localized template, then passes through a per-channel Redis token-bucket rate limiter sized for the upstream provider's limits. After sending, the provider callbacks (delivery receipts, bounces, complaints) feed back into a status updater that marks each notification sent, delivered, or failed, and increments a bounce-rate metric. If bounce rate exceeds half a percent the system alerts. Exhausted retries go to a dead-letter queue."
      }
    },
    {
      "type": "prose",
      "id": "fan-out",
      "payload": {
        "text": "**Deep dive: fan-out and per-channel rate limiting.**\n\nThe whole point of the design is that a single semantic event becomes one or more channel deliveries, each with its own delivery semantics. We do the fan-out as late as possible: the producer says \"send to channels [push, email]\", the API persists one notification row per channel, and a separate Kafka consumer pool per channel does the actual sending.\n\n**Why separate worker pools per channel?** Push, email, and SMS have wildly different throughput, latency, and failure characteristics. SMS is slow and expensive; if Twilio is down, we must not stall push delivery. By partitioning consumers per channel, a Twilio outage backpressures only the SMS queue, not the entire system. This is the **bulkhead pattern**.\n\n**Per-channel rate limiting.** Each provider publishes a rate limit we must not exceed. We implement a Redis token bucket per provider credential (one per APNS cert, one per FCM project, one per SES region, one per Twilio sender). The Lua script atomically decrements tokens and returns whether the request fits. If it doesn't fit, the worker sleeps with backoff or re-enqueues the message to a delay queue. This guarantees we never blow past provider limits and get throttled.\n\n**Priority queue for transactional.** Marketing notifications should not crowd out \"your ride is here\". The push fan-out has a two-tier priority queue: tier 1 (transactional, OTP, security) is drained first; tier 2 (marketing) is drained only when tier 1 is below a depth threshold. We also cap marketing throughput at 10% of provider capacity so a Black Friday campaign never starves OTP delivery.\n\n**Bounce and complaint handling.** Email providers (SES, SendGrid) deliver webhooks for bounces, complaints, and unsubscribes. We record these against the user and auto-suppress future emails to that address after one hard bounce or one complaint. Failing to do this is the #1 way to get your SES account suspended."
      }
    },
    {
      "type": "prose",
      "id": "dedup-and-quiet",
      "payload": {
        "text": "**Deep dive: deduplication, preferences, and quiet hours.**\n\n**Idempotency.** Producers retry. Without dedup, an OTP resend would deliver twice and confuse the user. We hash (idempotency_key, channel) and check a Redis SET with 24-hour TTL. On a hit we silently drop the duplicate and increment a `dedup_drop` metric. This is the same idempotency pattern as Stripe's idempotency keys — producers must generate the key deterministically (e.g. `sha256(event_id + channel)`).\n\n**Preferences.** Every notification event has a `category` (security, transactional, social, marketing). The user's preference record maps category -> enabled channels. Security and transactional categories are non-suppressible (the user cannot opt out of OTP SMS — that's a security requirement). Marketing is fully user-controlled and respects unsubscribe links per CAN-SPAM / GDPR.\n\n**Quiet hours.** Users can set a local-time window (e.g. 22:00-07:00) during which push notifications are buffered. The buffer is a Redis sorted set keyed by delivery-time; a sweeper releases them at 07:00 local. Urgent notifications (security alerts) bypass quiet hours. We store the user's timezone on the preference record; without it we cannot compute local time.\n\n**Template rendering.** Each channel and locale has a Jinja2 template. Variables are passed in the event payload; missing variables are rendered as empty rather than crashing. Templates are versioned — a render with v3 of a template is deterministic, so historical notifications can be re-rendered identically. Localization includes not just translation but also date/number formatting (e.g. \"12 hours ago\" in German vs Japanese)."
      }
    },
    {
      "type": "prose",
      "id": "bottlenecks",
      "payload": {
        "text": "**Bottlenecks and failure modes.**\n\n- **Producer storm.** A buggy producer emits 1M notifications in 1 minute. *Mitigation*: per-producer rate limits at the API layer (token bucket per service_id); hard cap on marketing category throughput.\n\n- **APNS/FCM throttle.** Pushing 50K/sec will get rate-limited. *Mitigation*: token-bucket at the provider limit; backpressure to Kafka; APNS supports a `collapse-id` to coalesce notifications of the same type for the same device.\n\n- **Email bounce spike.** A bad list causes 10% bounce rate, SES suspends the account. *Mitigation*: auto-suppress after one bounce; alert at 0.5% bounce rate; warm up new sender domains gradually.\n\n- **Twilio outage.** SMS workers retry with exponential backoff until the message expires (default 5 min). *Mitigation*: separate bulkhead — push and email still flow. After 5 min, dead-letter the message and surface to ops.\n\n- **Hot recipient.** A user mentions 500 people in a single Slack message; 500 fan-out writes for one event. *Mitigation*: batch by recipient_id; coalesce multiple recent notifications to the same user into one digest.\n\n- **Notification fatigue.** A user gets 50 pushes in an hour from the same product and disables notifications app-wide. *Mitigation*: per-recipient-per-category rate limit (\"at most 3 social pushes per hour\"); digest mode.\n\n- **Template regressions.** A bad template pushes broken HTML emails to 1M users. *Mitigation*: render to a small canary list (internal team) first; require a manual promote step to go to 100%."
      }
    },
    {
      "type": "prose",
      "id": "scaling",
      "payload": {
        "text": "**Scaling strategy and trade-offs.**\n\n*Kafka partitioning.* Partition the notifications topic by `recipient_id` so a given user's notifications serialize through one partition (preserves order, enables per-user dedup). 64 partitions scales to ~100K events/sec with headroom.\n\n*Worker autoscaling.* Each consumer pool (push, email, SMS) autoscales on consumer lag. Email workers batch send (SES accepts up to 50 destinations per SendEmail call) which reduces cost 50x for marketing.\n\n*Multi-region.* Deploy the full stack in 2-3 regions. The API is region-affinity (user's region determined by JWT). Kafka MirrorMaker replicates the notification topic cross-region for DR. Provider credentials are per-region where possible (SES region-specific).\n\n*Trade-offs made explicit.*\n- We chose at-least-once delivery with idempotent consumers — **gained** simplicity and guaranteed delivery, **lost** true exactly-once (acceptable since dedup covers the common case).\n- We chose per-channel worker pools — **gained** bulkhead isolation, **lost** some efficiency (a quiet push worker can't help a backed-up SMS worker).\n- We chose priority queue for transactional — **gained** OTP always wins, **lost** some marketing throughput under load (intentional).\n- We chose to fan out late (after Kafka) — **gained** replayability (events stay in Kafka for 7 days), **lost** a bit of latency (one extra hop).\n- We chose 24h dedup TTL — **gained** protection against producer retry storms, **lost** the ability to legitimately send the same notification twice in 24h (acceptable; producers should generate distinct events)."
      }
    },
    {
      "type": "quiz",
      "id": "q1",
      "payload": {
        "question": "A producer retries the same notification event 3 times in 5 minutes. How do you avoid sending 3 SMSes?",
        "shape": "mcq",
        "options": [
          "Hash (idempotency_key, channel) into a Redis SET with 24h TTL; on a hit, silently drop the duplicate.",
          "Use exactly-once Kafka delivery, which guarantees no duplicates.",
          "Store a 'sent' flag on the event in Kafka and check before sending.",
          "Rely on Twilio's idempotency API to dedup on their side."
        ],
        "answer_index": 0,
        "rationale": "Exactly-once delivery across Kafka + worker + external provider is a myth; we use at-least-once plus idempotent consumers. The producer must generate an idempotency_key deterministically (e.g. sha256(event_id + channel)); the notification service checks a Redis SET with 24h TTL on (idempotency_key, channel) before persisting/sending. On a hit, we drop and increment a metric. Option B is wrong because Kafka exactly-once only covers the Kafka consumer offset commit, not the side-effecting send to Twilio. Option C doesn't survive across consumer rebalances or restarts. Option D is wrong — Twilio has no such API; SMS is inherently non-idempotent at the protocol level.",
        "difficulty": "interview"
      }
    },
    {
      "type": "quiz",
      "id": "q2",
      "payload": {
        "question": "Twilio is having an outage. What happens to push notifications and emails?",
        "shape": "mcq",
        "options": [
          "Nothing — separate per-channel worker pools (bulkheads) isolate them; only SMS queue backs up.",
          "Push and email also slow down because the worker pool is shared.",
          "The whole notification API starts returning 503 because the SMS worker can't drain.",
          "Push and email are paused until Twilio recovers to preserve ordering."
        ],
        "answer_index": 0,
        "rationale": "Each channel has its own consumer pool reading from a logically separate queue. This is the bulkhead pattern: a failure in one provider's path backpressures only that channel. SMS workers retry with exponential backoff up to a 5-minute TTL, then dead-letter. Push and email continue flowing through their own pools. Option B is the most common mistake (single shared worker pool) — that design couples all channels to the slowest provider, which defeats the whole point of separating channels. Option C is wrong because the API only produces to Kafka; it doesn't block on downstream send. Option D is wrong because we explicitly do not preserve cross-channel ordering.",
        "difficulty": "interview"
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-design-notification-system-mental",
      "payload": {
        "text": "Engineering mental model",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-design-notification-system-mental",
      "payload": {
        "text": "**Mental model.** Think of Design Notification System as a deliberate boundary in a system. The boundary exists because something becomes harder to manage when everything is done in one place: latency, scale, failure isolation, consistency, cost, or team ownership. The useful question is not “what does Design Notification System mean?” but “what pressure makes this boundary worth introducing, and what new failure mode does it create?”"
      }
    },
    {
      "type": "callout",
      "id": "upgrade-callout-design-notification-system-decision",
      "payload": {
        "title": "Design lens",
        "body": "Before choosing Design Notification System, name the workload, the critical user path, the dominant bottleneck, the failure you are trying to absorb, and the trade-off you are willing to accept. If you cannot name those five things, the design is probably premature.",
        "kind": "info"
      }
    },
    {
      "type": "image",
      "id": "upgrade-image-design-notification-system",
      "payload": {
        "src": "/learning-visuals/design-notification-system.svg",
        "alt": "Original NO CAP systems visual for Design Notification System.",
        "caption": "Design Notification System: a compact system-thinking visual.",
        "credit": "Original NO CAP visual.",
        "source_url": "https://github.com/donnemartin/system-design-primer"
      }
    },
    {
      "type": "mermaid",
      "id": "upgrade-mermaid-design-notification-system",
      "payload": {
        "code": "flowchart LR\n  A[Client] --> B[Gateway] --> C[Services] --> D[Data] --> E[Delivery]",
        "caption": "Design Notification System: a simplified system view showing the primary flow and the main state boundaries.",
        "alt_text": "Design Notification System: a simplified system view showing the primary flow and the main state boundaries."
      }
    },
    {
      "type": "code",
      "id": "upgrade-code-design-notification-system",
      "payload": {
        "language": "text",
        "code": "// Pseudocode\nrequest = receive()\nresult = design_notification_system(request)\nreturn result\n\n// Production questions:\n// 1. What happens on timeout?\n// 2. Can this operation be retried safely?\n// 3. What is the bottleneck?",
        "caption": "A minimal engineering sketch for reasoning about Design Notification System."
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-design-notification-system-numbers",
      "payload": {
        "text": "Back-of-the-envelope reasoning",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-design-notification-system-numbers",
      "payload": {
        "text": "Numerical lens: write down traffic, payload size, read/write ratio, peak multiplier and durability target before choosing a component. The numbers should justify the architecture."
      }
    },
    {
      "type": "simulation",
      "id": "upgrade-sim-design-notification-system",
      "payload": {
        "title": "Interactive thought experiment: Design Notification System",
        "description": "Change the variables below and predict what breaks first in Design Notification System. The production lab can later reuse these same inputs.",
        "variables": [
          {
            "name": "traffic",
            "description": "Requests/events per second",
            "default": "1000"
          },
          {
            "name": "latency",
            "description": "Dependency latency",
            "default": "50ms"
          },
          {
            "name": "failure_rate",
            "description": "Injected failure rate",
            "default": "0%"
          }
        ],
        "placeholder_note": "Interactive controls are deterministic; use the linked Labs section when a dedicated lab exists."
      }
    },
    {
      "type": "callout",
      "id": "upgrade-callout-design-notification-system-hint",
      "payload": {
        "title": "Hint",
        "body": "If you are stuck on Design Notification System, start by drawing the request path and marking every network hop, stateful component, queue, cache and failure boundary. Then estimate where the system will saturate.",
        "kind": "note"
      }
    },
    {
      "type": "quiz",
      "id": "upgrade-quiz-design-notification-system-1",
      "payload": {
        "shape": "prediction",
        "question": "You increase traffic by 10× in a system using Design Notification System. What should you inspect first?",
        "options": [
          "The newest framework version",
          "The dominant bottleneck and its saturation signal",
          "Only the UI",
          "Nothing; the architecture should absorb any traffic automatically"
        ],
        "answer_index": 1,
        "rationale": "System design is a constraint-and-bottleneck exercise. Start with the resource or dependency that will saturate first, then choose the smallest change that removes that bottleneck.",
        "difficulty": "solid"
      }
    },
    {
      "type": "quiz",
      "id": "upgrade-quiz-design-notification-system-2",
      "payload": {
        "shape": "mcq",
        "question": "Which statement is the safest engineering habit when using Design Notification System?",
        "options": [
          "Choose it because it is popular",
          "Add it before measuring the problem",
          "State the workload, failure mode and trade-off it is solving",
          "Use the most distributed design possible"
        ],
        "answer_index": 2,
        "rationale": "Good architecture starts from requirements, workload and failure assumptions. The component is the consequence of that reasoning, not the starting point.",
        "difficulty": "interview"
      }
    },
    {
      "type": "scenario",
      "id": "upgrade-scenario-design-notification-system",
      "payload": {
        "prompt": "Production scenario: your system uses Design Notification System, traffic suddenly spikes, and p99 latency doubles. What is your first move?",
        "context": "You have dashboards for traffic, latency, errors and saturation. You can change the architecture, but every change has operational cost.",
        "options": [
          {
            "id": "a",
            "text": "Add more instances immediately",
            "outcome": "This may help, but only if compute saturation is the bottleneck.",
            "correct": false
          },
          {
            "id": "b",
            "text": "Inspect the bottleneck and its saturation signal first",
            "outcome": "Correct: diagnose before scaling or introducing a new pattern.",
            "correct": true
          },
          {
            "id": "c",
            "text": "Disable monitoring to reduce overhead",
            "outcome": "This removes the evidence you need to diagnose the incident.",
            "correct": false
          }
        ],
        "rationale": "Measure the limiting resource or dependency first. A bottleneck can be compute, database I/O, network, cache misses, lock contention, downstream latency, or queue backlog.",
        "difficulty": "interview"
      }
    },
    {
      "type": "heading",
      "id": "upgrade-heading-design-notification-system-interview",
      "payload": {
        "text": "Interview drill",
        "level": 2
      }
    },
    {
      "type": "prose",
      "id": "upgrade-prose-design-notification-system-interview",
      "payload": {
        "text": "**Answer this without notes:** When would you choose Design Notification System, and when would you intentionally avoid it? Mention at least one bottleneck it addresses, one failure mode it introduces, and one alternative. Then quantify the workload you are designing for."
      }
    },
    {
      "type": "callout",
      "id": "field-guide-design-notification-system",
      "payload": {
        "title": "Engineering lens",
        "body": "A useful engineering lens for Design Notification System: define the problem it solves, the simpler design that fails first, the constraint that forces you to introduce this concept, and the new failure modes the concept creates.",
        "kind": "tip"
      }
    },
    {
      "type": "prose",
      "id": "numerical-lens-design-notification-system",
      "payload": {
        "text": "### Numerical sanity check\n\nBack-of-the-envelope reasoning beats fake precision. State your traffic, payload, concurrency and growth assumptions explicitly, then calculate enough to know whether the current architecture is orders of magnitude away from the target."
      }
    },
    {
      "type": "quiz",
      "id": "field-question-design-notification-system",
      "payload": {
        "shape": "prediction",
        "question": "Imagine the simplest version of a system using Design Notification System. What breaks first as traffic grows by 10×, and what would you change before reaching 100×?",
        "context": "Do not optimize for a memorized definition. Reason from the workload and failure mode.",
        "options": [
          "Scale the same component immediately.",
          "Measure the current bottleneck and state the assumption that changed.",
          "Add more technologies until throughput improves.",
          "Choose the most popular architecture."
        ],
        "answer_index": 1,
        "rationale": "Good system design starts with the bottleneck and the changed constraint. The most popular or complex choice is not automatically correct.",
        "difficulty": "interview"
      }
    }
  ],
  "trade_offs": {
    "pros": [
      "Per-channel worker pools isolate provider failures (bulkhead pattern).",
      "Idempotency keys with Redis SET dedup make at-least-once delivery practical.",
      "Priority queue ensures transactional notifications always beat marketing.",
      "Late fan-out (after Kafka) keeps events replayable for 7 days."
    ],
    "cons": [
      "At-least-once requires every downstream side-effect to be idempotent (extra burden on consumer code).",
      "Per-channel worker pools cannot share idle capacity across channels.",
      "24h dedup TTL prevents legitimate same-day re-sends (producers must mint distinct events).",
      "Template + localization versioning adds operational complexity."
    ]
  },
  "failure_modes": [
    "Producer storm floods Kafka — needs per-producer rate limits at the API.",
    "APNS/FCM throttle from exceeded throughput — needs token-bucket rate limiter per credential.",
    "SES suspension from bounce spike — needs auto-suppression after one bounce and alert at 0.5%.",
    "Twilio outage backs up SMS — bulkhead isolation + retry + dead-letter after TTL.",
    "Hot recipient floods fan-out — needs per-recipient-per-category rate limit + digest mode.",
    "Template regression sends broken emails — needs canary render before 100% rollout."
  ],
  "common_mistakes": [
    "Single shared worker pool for all channels — a slow SMS provider stalls push.",
    "No idempotency key — producers retry and send duplicate SMSes / push notifications.",
    "Treating Kafka exactly-once as covering external side-effects — it only covers offsets.",
    "Forgetting to honor quiet hours or locale when rendering templates.",
    "Not auto-suppressing bounced email addresses — gets SES account suspended.",
    "Sending marketing pushes through the same priority lane as OTPs — OTPs get starved."
  ],
  "where_you_see_it": [
    "Uber trip-status notifications (push + SMS as backup)",
    "Slack @-mention notifications across desktop, mobile, email",
    "Airbnb booking confirmations (multi-channel fan-out)",
    "Stripe receipts and dispute alerts",
    "GitHub CI/CD result notifications"
  ],
  "interview_prompts": [
    "Design a notification system that sends push, email, and SMS. How do you handle a Twilio outage?",
    "A producer is retrying the same notification 3 times. How do you avoid duplicate SMS delivery?",
    "How do you make sure OTP SMSes always beat marketing pushes under load?",
    "Your email bounce rate just hit 5% and SES is about to suspend you. What do you do?",
    "How would you add a digest mode that batches 10 social notifications into one email per hour?"
  ],
  "real_system_mappings": [
    {
      "system": "Uber",
      "how": "Internal notification platform ('Floodgate') with per-channel worker pools, idempotency keys, and a priority lane for trip-status pushes above marketing. Documented in Uber Engineering blog posts."
    },
    {
      "system": "Slack",
      "how": "Notifications service routes each event to desktop, mobile, and email channels based on per-user preferences and presence state; uses Kafka + per-channel workers."
    },
    {
      "system": "Airbnb",
      "how": "Notification service fans out booking events to push, SMS (Twilio), and email (SES); template rendering with locale; per-channel rate limits; documented in Airbnb Medium posts."
    }
  ],
  "status": "published",
  "sources": [
    {
      "title": "System Design Primer",
      "url": "https://github.com/donnemartin/system-design-primer",
      "publisher": "Open source"
    },
    {
      "title": "ByteByteGo — Scale from zero to millions",
      "url": "https://bytebytego.com/courses/system-design-interview/scale-from-zero-to-millions-of-users",
      "publisher": "ByteByteGo"
    },
    {
      "title": "System Design Tutorial",
      "url": "https://www.geeksforgeeks.org/system-design/system-design-tutorial/",
      "publisher": "GeeksforGeeks"
    },
    {
      "title": "System Design Roadmap",
      "url": "https://roadmap.sh/system-design",
      "publisher": "roadmap.sh"
    },
    {
      "title": "Case Studies reference",
      "url": "https://github.com/donnemartin/system-design-primer",
      "publisher": "Reference"
    },
    {
      "title": "Case Studies reference",
      "url": "https://bytebytego.com/courses/system-design-interview/scale-from-zero-to-millions-of-users",
      "publisher": "Reference"
    },
    {
      "title": "Case Studies reference",
      "url": "https://www.geeksforgeeks.org/system-design/system-design-tutorial/",
      "publisher": "Reference"
    }
  ]
}
